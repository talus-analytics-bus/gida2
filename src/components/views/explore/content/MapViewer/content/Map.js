import React from "react";
import styles from "./map.module.scss";

// FC for Map.
const Map = ({ ...props }) => {
  return <div className={styles.map}>Map area placeholder</div>;
};

export default Map;
